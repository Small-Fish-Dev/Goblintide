@using Sandbox.UI;

@namespace GameJam.UI
@inherits SmartPanel<RaidCompleted>
@attribute [StyleSheet( "/UI/Raiding/Completed/RaidCompleted.scss" )]

@code {

	public RaidCompleted()
	{
		// Make sure RaidInProgress is gone
		RaidInProgress.Delete();
	}

}
